// Q15: Sum of diagonal elements of a matrix
#include <stdio.h>
int main(){
    int n,i,sum=0,j;
    scanf("%d",&n);
    int a[n][n];
    for(i=0;i<n;i++) for(j=0;j<n;j++) scanf("%d",&a[i][j]);
    for(i=0;i<n;i++) sum+=a[i][i];
    printf("Sum: %d",sum);
    return 0;
}
