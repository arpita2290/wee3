// Q17: program for sum of all odd numbers in an array
#include <stdio.h>
int main() {
    int n,i,sum=0;
    scanf("%d",&n);
    int arr[n];
    for(i=0;i<n;i++) {scanf("%d",&arr[i]); if(arr[i]%2) sum+=arr[i];}
    printf("Odd element sum: %d",sum);
    return 0;
}
